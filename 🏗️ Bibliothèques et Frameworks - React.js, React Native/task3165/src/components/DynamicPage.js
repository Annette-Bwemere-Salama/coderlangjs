import React from "react";

const DynamicPage = () => {
    return (
        <>
            <h4>{pageHeading}</h4>
            <p>{pageDecription}</p>
        </>
    );
};

export default DynamicPage;
